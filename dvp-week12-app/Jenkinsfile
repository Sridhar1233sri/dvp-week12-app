pipeline {
    agent any

    stages {

        stage('Build & Test') {
            steps {
                sh '''
                python3 --version
                pip3 install -r dvp-week12-app/requirements.txt
                pytest dvp-week12-app
                '''
            }
        }

    }
}
