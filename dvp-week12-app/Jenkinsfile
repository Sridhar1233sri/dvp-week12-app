pipeline {
    agent any

    stages {

        stage('Build & Test') {
            steps {
                sh '''
                python3 --version
                pip3 install --user -r dvp-week12-app/requirements.txt
                python3 -m pytest dvp-week12-app
                '''
            }
        }

    }
}
